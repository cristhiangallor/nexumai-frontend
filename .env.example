# Variables de entorno del frontend (Vite). Copiar a `.env.local` y ajustar.
# Solo las variables con prefijo VITE_ se exponen al código cliente.

# Base URL del backend por entorno. Sin barra final.
VITE_API_BASE_URL=http://localhost:8080
